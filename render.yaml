services:
  - type: web
    name: lecture-ticket-management-system
    runtime: python
    plan: free
    buildCommand: python -m py_compile server.py
    startCommand: python server.py
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
